services:
  server:
    volumes:
      - .:/app # live code sync

    command: >
      sh -c "gunicorn config.wsgi:application
      --bind 0.0.0.0:${PORT:-8000}
      --workers 4
      --reload
      --access-logfile -"

/**
 * Platform dummy data removed — conversations and agents use live org data.
 * Kept as a thin stub so any leftover imports fail clearly at typecheck.
 */

export const USE_PLATFORM_DUMMY_DATA = false;

export const PLATFORM_DUMMY_DATA_BANNER = "";

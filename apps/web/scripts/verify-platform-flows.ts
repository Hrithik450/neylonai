/**
 * Platform flow verification — conversations are live-only now.
 * Ticket / demo inbox fixtures were removed with the ticket system.
 */

console.log("verify-platform-flows: ticket/demo fixtures removed.");
console.log("Use the dashboard Conversations page against a live org instead.");
process.exit(0);
